<UserControl
    x:Class="TaskManagerApp.BtnControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskManagerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Name="BtnUC">
    <UserControl.Resources>
        <local:BoolToColourConverter x:Key="BoolToColour"/>
    </UserControl.Resources>
    <Grid>
        <Button Name="StarBtnDetails"
                      FontFamily="Segoe MDL2 Assets" Grid.Row="1" Grid.Column="1"
                      Content="&#xE734;" 
                      Style="{StaticResource TextBlockButtonStyle}"
                     Tag="{Binding ZFav.TaskId,Mode=OneWay,ElementName=BtnUC}"
                     Click="StarBtn_Click"
                    Background="{Binding ZFav.IsFavourite,Mode=OneWay,ElementName=BtnUC,Converter={StaticResource BoolToColour},UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
