<Page
    x:Class="TaskManagerApp.ViewMyTaskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskManagerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <RelativePanel>
        <ListView Name="TasksList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Background="LightSkyBlue" HorizontalAlignment="Stretch"
                        Margin="0,10,0,0">
                        <TextBlock Text="{Binding TaskName}"
                                  FontWeight="Black"
                                Margin="0,10,0,0"
                                FontFamily="Calibri"
                                FontSize="17"/>
                        <TextBlock Text="{Binding AssignedByUser}"
                                    FontStyle="Italic"/>
                        <TextBlock Text="{Binding TaskId}"
                                   Foreground="Crimson"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </RelativePanel>
</Page>
